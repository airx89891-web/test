
# Wafer Die Map Editor (Streamlit)
此專案提供互動式介面，用於：
- 上傳 XML 檔案
- 設定 Wafer Size、Die Size、Offset
- 支援多 Die 類型設定並對應不同 Die
- 更新 XML 並下載
- 視覺化 wafer 外框、Die 分布、格線

## 本機執行
```bash
pip install -r requirements.txt
streamlit run app.py
